{
    "extends": "../../tslint.base.json",
    "rules": {
        // We don't want vscode dependencies (except vscode-languageserver-types)
        "import-blacklist": [true, "vscode", "vscode-languageserver"]
    }
}
